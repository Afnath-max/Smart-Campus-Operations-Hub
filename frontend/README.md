# Frontend

This folder contains the React + Vite client for Smart Campus Operations Hub.

- Main setup and deployment instructions live in the root [`README.md`](../README.md)
- Environment variables are documented in [`frontend/.env.example`](.env.example)
- The production container uses [`frontend/Dockerfile`](Dockerfile) and [`frontend/nginx.conf`](nginx.conf)
